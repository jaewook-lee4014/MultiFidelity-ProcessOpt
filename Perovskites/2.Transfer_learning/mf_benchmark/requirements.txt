# Multi-Fidelity Bayesian Optimization Benchmark
# Python >= 3.8

# =============================================================================
# Installation
# =============================================================================
#
# Standard (x86_64 with CUDA):
#   pip install -r requirements.txt
#
# ARM64 (GH200, Grace Hopper):
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
#
# CPU only:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#
# =============================================================================

# Core Scientific Computing
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0
matplotlib>=3.4.0

# Machine Learning
scikit-learn>=1.0.0

# PyTorch (install separately for specific architecture - see above)
torch>=2.0.0

# Bayesian Optimization (BoTorch + GPyTorch)
botorch>=0.9.0
gpytorch>=1.10.0

# Note: The following are used but come with Python standard library:
# - pathlib, datetime, typing, argparse, multiprocessing, time, os, warnings
